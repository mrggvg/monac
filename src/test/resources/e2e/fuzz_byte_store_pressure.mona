// expect: A=1
//
// Found by the program fuzzer (seed 20360940): storing a byte member of a struct
// initializer through a spilled base, with B and C both holding live values, left no
// register to carry the byte — and the selector threw rather than borrowing one.

struct S0 { word m0; byte m1; byte m2; };
word g1;

word main() {
    word v4 = (0 < (g1 ? 0 : 32768));
    struct S0 s5 = {(1 <= v4), (g1 | g1), (v4 ? v4 : v4)};
    if (s5.m0 != 1 || s5.m1 != 0 || s5.m2 != 1) return 99;
    return ((v4 / (v4 | 1)) + ((sbyte)32768));
}
